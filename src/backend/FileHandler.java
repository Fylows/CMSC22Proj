package backend;

import java.nio.file.Path;

public class FileHandler {	
		/**
		 * loadCSV 
		 * a function that loads the dataset csv into a hash map of courses
		 */
		public static Object loadCSV () {
			return null;
			
		}
		
		/**
		 *  loadObject
		 * 
		 * a function that loads the saved courses file into a hash map of courses
		 * 
		 * @param	path where the file is located
		 * @return	returns the loaded Object from the file
		 */
		public static Object loadObject (Path path) {
			return null;
		}
		
		/**
		 *  saveObject
		 * 
		 * a function that saves the current state of the courses into a hash map of courses
		 * 
		 * @param	path where the file will be saved
		 */ 
		public void saveObject (Path path) {
			
		}
}
